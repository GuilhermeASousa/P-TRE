<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>


    <!-- For all other devices -->
    <!-- Size should be 32 x 32 pixels -->
    <!--<%= favicon_link_tag 'favicon.png', :rel => 'shortcut icon', :type => 'image/png' %>-->

    <%= javascript_include_tag "application" %>

    <style>
    body { padding-top: 70px; }
    div#tudo { min-height: 530px;}
    a.disabled {
      pointer-events: none;
      cursor: default;
    }
    </style>

  </head>
  <body>

    <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="#"><font size="3"></font></a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav">
            <% if @here == 1 %>
              <li class="active">
            <% else %>
              <li>
            <% end %>
              <a href="/">
                <font size="3">Home</font>
                <span class="sr-only">(current)</span>
              </a>
            </li>
            <% if @here == 3 %>
              <li class="active">
                <a href="/resultado">
                <font size="3">Resultado</font>
                </a>
              </li>
            <% end %>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <% if @here == 2 %>
              <li class="active">
            <% else %>
              <li>
            <% end %>
              <%= link_to "Sobre", {:controller => "about", :action => "about"}, {:style=>'size:3'} %>
            </li>
          </ul>
        </div><!-- /.navbar-collapse -->
      </div><!-- /.container-fluid -->
    </nav>

    <div class="container">
      <div id="tudo" class="row">
        <div class="span12">
          <%= bootstrap_flash %>
          <%= yield %>
        </div>
      </div><!--/row-->



      <footer>
        <br><center><p>&copy; Universidade Federal de Minas Gerais</p></center>
      </footer>

    </div> <!-- /container -->

  </body>
</html>
